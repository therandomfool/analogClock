# JS Analog Desktop Clock Prototype

- added current date-time text string with toString()
- js functions getSeconds(), getMinutes(), getHours()
- position clockhand(s) every second~~ setInterval(updateClock, 1000) 

